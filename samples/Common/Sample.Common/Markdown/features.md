## Framework Features

MvvmNav provides a lightweight navigation system for MVVM applications.

ⓘ The ViewModel of this page is derived from `ViewModelBase` which gives it `Title` and `IsBusy` properties. 


### Core features

Select a feature to learn more.

ⓘ The `NavigationService` will then show a detail page and pass parameters to it. 
