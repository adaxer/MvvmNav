<UserControl x:Class="ADaxer.MvvmNav.Sample.Wpf.Views.DetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADaxer.MvvmNav.Sample.Wpf.Views"
             xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource RoundButton}">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="0 -4 0 0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Orientation="Horizontal" Margin="-40 0 0 20">
            <Button Content="←" Command="{Binding PreviousCommand}" ToolTip="Previous Feature - this shows the navigation to the same DataType - normally forbidden - but possible with different parameters"/>
            <Button  Content="→" Command="{Binding NextCommand}" ToolTip="Previous Feature - this shows the navigation to the same DataType - normally forbidden - but possible with different parameters"/>
        </StackPanel>
        <markdig:MarkdownViewer Markdown="{Binding Markdown}"/>
    </DockPanel>
</UserControl>
