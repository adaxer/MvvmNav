<UserControl x:Class="ADaxer.MvvmNav.Sample.Wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADaxer.MvvmNav.Sample.Wpf.Views"
             xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="10" Background="#FDD">
            <CheckBox VerticalAlignment="Center" Content="Check or uncheck to change state" IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="0,0,8,0"/>
            <TextBlock VerticalAlignment="Center" Text="Enter text:" Margin="0,0,8,0"/>
            <TextBox Background="Transparent" VerticalAlignment="Center" Text="{Binding State, Mode=TwoWay}" />
        </DockPanel>
        <markdig:MarkdownViewer Markdown="{Binding Markdown}"/>
    </DockPanel>
</UserControl>
